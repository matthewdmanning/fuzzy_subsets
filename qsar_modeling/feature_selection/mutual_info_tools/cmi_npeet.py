import numpy as np

from feature_selection.mutual_info_tools.npeet_lnc import MI

'''
get_lnc_mi(x, y, k, alpha=0.25, base=np.exp(1)):
return MI.mi_LNC([x, y], k=5, base=base, alpha=alpha)  # Apply LNC estimator
'''
